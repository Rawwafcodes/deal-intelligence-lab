# Prompt to send with this folder

Read every file in this package completely, then inspect the live repository and its instruction files before acting.

This package integrates the “Multiplayer Diligence System” contribution into the existing workspace-shift roadmap. It does not authorize an immediate contradiction-engine build or a rewrite of working modules.

Your task is documentation adoption and roadmap reconciliation only:

1. Verify the package's current-state summary against the live code, database/schema state, git state and canonical `STATUS.md`.
2. Identify any conflict between this package, the live implementation and existing product decisions.
3. Adopt the product integration into the canonical workspace-shift documentation using the repository's established naming and append-only status/decision conventions.
4. Update the canonical roadmap so:
   - M13.2 remains next.
   - M13.3 and M13.4 complete the collaborative substrate.
   - M14.2 becomes the first Work-product Integrity Review implementation.
   - M15 gains dependency tracking, targeted reassessment and opt-in triggered Integrity Review.
   - A gated M16 covers the evidence ledger, deterministic reconciliation and continuous cross-workstream integrity.
5. Preserve all completed M1–13.1 functionality and history.
6. Do not implement M14–M16, add dependencies, migrate data, make paid API calls, deploy, commit or push during adoption.
7. Draft the next implementation task only if the live roadmap does not already contain an adequate M13.2 task. Otherwise report whether the existing M13.2 task needs any small lineage hooks for future Integrity Review.
8. Run the package checker and any documentation validation already present in the repository.

Return:

- Adoption report.
- Files changed.
- Conflicts found.
- Decisions added or proposed.
- Tests/checks run.
- Confirmation that M13.2 remains next.
- Any permissions required before implementation.

